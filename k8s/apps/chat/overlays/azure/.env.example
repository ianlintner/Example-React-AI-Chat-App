# Azure AKS Deployment - Environment Configuration
# Copy this file to .env and update with your values

# ===== Azure Configuration =====
# Resource group where resources will be created
AZURE_RESOURCE_GROUP=ai-chat-rg

# AKS cluster name
AZURE_CLUSTER_NAME=ai-chat-aks

# Azure region (e.g., eastus, westus2, westeurope)
AZURE_LOCATION=eastus

# Azure Container Registry name (must be globally unique, lowercase alphanumeric only)
# Example: aichatacr, mycompanyaichat, aichat20231119
AZURE_ACR_NAME=aichatacr

# Number of nodes in the AKS cluster
AZURE_NODE_COUNT=2

# VM size for nodes
# Development: Standard_B2s (~$30/month per node)
# Production: Standard_D2s_v3 (~$70/month per node)
# See: https://azure.microsoft.com/en-us/pricing/details/virtual-machines/
AZURE_NODE_SIZE=Standard_D2s_v3

# ===== Application Configuration =====
# Your domain for the application (optional, can use IP initially)
APP_DOMAIN=chat.yourdomain.com

# ===== Secrets =====
# These should be managed separately and not committed to git
# Option 1: Set as environment variables
# Option 2: Use Azure Key Vault
# Option 3: Create secrets.yaml from secrets.yaml.example

# OpenAI API Key (optional - app works without it using RAG mode)
# OPENAI_API_KEY=sk-...

# Redis Password (for in-cluster Redis)
# REDIS_PASSWORD=your-secure-redis-password

# ===== Optional Configuration =====
# Enable automatic cluster scaling
# ENABLE_CLUSTER_AUTOSCALER=true
# MIN_NODE_COUNT=1
# MAX_NODE_COUNT=5

# Enable Azure Monitor Container Insights (enabled by default)
# ENABLE_MONITORING=true

# Network plugin (azure or kubenet)
# NETWORK_PLUGIN=azure

# DNS service IP for the cluster
# DNS_SERVICE_IP=10.0.0.10

# Docker bridge network address
# DOCKER_BRIDGE_ADDRESS=172.17.0.1/16
