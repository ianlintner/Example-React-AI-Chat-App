apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: chat-gateway-external
spec:
  selector:
    istio: aks-istio-ingressgateway-external
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "cat-herding.net"
  # Uncomment for HTTPS/TLS support when you have certificates configured
  # - port:
  #     number: 443
  #     name: https
  #     protocol: HTTPS
  #   tls:
  #     mode: SIMPLE
  #     credentialName: chat-backend-tls
  #   hosts:
  #   - "chat.yourdomain.com"
