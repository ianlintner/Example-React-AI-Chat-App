apiVersion: v1
kind: Service
metadata:
  name: chat-frontend
  labels:
    app: chat-frontend
spec:
  type: ClusterIP
  selector:
    app: chat-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
