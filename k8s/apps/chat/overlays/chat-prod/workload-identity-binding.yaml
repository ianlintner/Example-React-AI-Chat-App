apiVersion: iam.cnrm.cloud.google.com/v1beta1
kind: IAMPolicyMember
metadata:
  name: chat-prod-workload-identity
  namespace: chat-prod
spec:
  member: serviceAccount:YOUR_PROJECT_ID.svc.id.goog[chat-prod/chat]
  role: roles/iam.workloadIdentityUser
  resourceRef:
    apiVersion: iam.cnrm.cloud.google.com/v1beta1
    kind: IAMServiceAccount
    name: chat-prod-sa
